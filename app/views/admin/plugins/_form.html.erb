<%= form_with(model: [:admin, plugin], local: true) do |form| %>
  <% if plugin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plugin.errors.count, "error") %> prohibited this plugin from being saved:</h2>

      <ul>
      <% plugin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :is_activated %>
    <%= form.check_box :is_activated %>
  </div>

  <div class="field">
    <%= form.label :site_id %>
    <%= form.text_field :site_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
